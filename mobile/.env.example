# --- CẤU HÌNH API BACKEND ---
# Đường dẫn gốc tới server Backend. 
# Mặc định app sẽ tự động nhận diện (localhost cho iOS/Web và 10.0.2.2 cho Android).
# Bỏ comment dòng dưới và sửa link nếu bạn muốn trỏ tới server thật (VD: https://api.aerostride.com)
# EXPO_PUBLIC_API_URL=http://localhost:8080
