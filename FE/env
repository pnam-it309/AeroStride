# ==============================================================================
# AEROSTRIDE - FRONTEND ENVIRONMENT (.env)
# ==============================================================================

# API Configuration
VITE_API_URL=/api/v1
VITE_WS_URL=/ws

# Backend Proxy (used by vite.config.js dev server)
BACKEND_HOST=localhost
BACKEND_PORT=8080

# Application Settings
VITE_APP_NAME=AeroStride
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=development

# Server Configuration
FE_DEV_PORT=5173